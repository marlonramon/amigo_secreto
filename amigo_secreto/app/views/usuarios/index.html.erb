<div class="container ">

<fieldset>
  <legend>Lista dos Participantes</legend>

  <table class="table table-striped table-hover table-bordered" style="width:800px">
    <tr>
      <th style="width:630px">Apelido</th>
      <th></th>
    </tr>

  <% @usuarios.each do |usuario| %>
    <tr>
      <td><%= usuario.apelido %></td>
      <td><%= link_to 'Visualizar Sugestão', usuario , :class => 'btn btn-inverse btn-small', :style=> 'color:white' %></td>
      <!-- <td><%= link_to 'Edit', edit_usuario_path(usuario) %></td> -->
   <!--   <td><%= link_to 'Destroy', usuario, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
    </tr>
  <% end %>
  </table>

</fieldset>
<br />

<%= link_to 'Novo Participante' , new_usuario_path, :class => 'btn btn-primary', :style=> 'color:white' %>
